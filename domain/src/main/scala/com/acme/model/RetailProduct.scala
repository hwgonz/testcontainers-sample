package com.acme.model

import java.util.UUID

case class RetailProduct(
                          id: UUID,
                          name: String,
                          description: String
                        )
