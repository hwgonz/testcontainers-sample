package com.acme.model

case class StockItem(
                      id: Long,
                      productId: Long,
                      warehouseId: Long,
                      quantity: Long
                    )
